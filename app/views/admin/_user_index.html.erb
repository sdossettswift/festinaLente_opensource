<% @users.each do |user| %>
  <tr>
    <td><%= user.username %></td>
    <td><%= user.role %></td>
    <td><%= link_to "Edit", edit_user_path(user), remote: true, class: "btn btn-default" %></td>
  </tr>
<% end %>
